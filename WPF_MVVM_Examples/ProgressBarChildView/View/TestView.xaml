<UserControl x:Class="ProgressBarChildView.View.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProgressBarChildView.View"
             mc:Ignorable="d" 
             xmlns:ViewModel="clr-namespace:ProgressBarChildView.ViewModel"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModel:MainWindowViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Button Width="200" Height="50" Content="Child Window Open" Command="{Binding OnButtonClickCommand}"/>
    </Grid>
</UserControl>
